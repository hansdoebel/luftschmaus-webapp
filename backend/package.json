{
  "name": "luftschmaus-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "test": "vitest run --reporter=junit --outputFile=results.xml --reporter=default"
  },
  "dependencies": {
    "fastify": "^5.8.4",
    "pg": "^8.20.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "vitest": "^4.1.1"
  }
}
